#pragma once

/* 7.30. 한승규
* 노드(9개)와, 현재 트레일 컴포넌트가 남긴 궤적의 충돌여부를 확인해서
* 결과값(어떤 노드를 통과-충돌 하였는지)를 반환해주는 매니저
* 즉, 1 3 2 이런식으로 배열을 뱉을 예정임
* + 추가적으로, 패턴 영역(사각형) 밖으로 나간걸 판단해서
* 트레일을 끊어주는(감소시키는)역할도 겸함
* 패턴 관련된 관리는 여기서 처리할 예정
*/

class PatternManager {




};
