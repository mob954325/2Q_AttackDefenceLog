#include "ImguiModuleTest.h"

void ImguiModuleTest::ImguiRender()
{
	ImGui::Begin("Hello, world!"); // Create a window called "Hello, world!" and append into it.
	// ImGui::Text("This is some useful text.");      // Display some text (you can use a forat strings too)
	//ImGui::Text("id = %d", data->enemyID);
	//ImGui::Text(data->enemyDifficulty.c_str());
	//ImGui::Text("health = %d", data->enemyHealth);
	//ImGui::Text("dmg = %d", data->enemyDamage);
	//ImGui::Text("cooldown = %d", data->enemyCooldown);
	//ImGui::Text("enemySpiritamount = %d", data->enemySpiritamount);
	//ImGui::Text("enemyGuardRate = %d", data->enemyGuardRate);

	// for (int i = 0; i <= 15; i++) {
	// 	ImGui::Text("enemyPattern = %d", data->enemyPattern[i]);
	// }
	ImGui::End();
}